import 'package:flutter/material.dart';

class OrganizationHeaderWidget extends StatelessWidget {
  const OrganizationHeaderWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}